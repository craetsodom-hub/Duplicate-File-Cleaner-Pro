# Phase 14 premium scan corpus

This committed corpus is copied to a disposable local directory before every integration test so Windows assigns real, independent physical file identities. It intentionally contains exact pairs across source roots, nested pairs, custom extensions, excluded folders, excluded extensions, and same-size near misses.
